version: '3.8'

services:
  newt:
    image: fosrl/newt
    container_name: newt
    restart: unless-stopped
  dubrecord:
    build: .
    env_file:
      - stack.env
    container_name: dubrecord
    environment:
      MY_ENVIRONMENT_VARIABLE: ${MY_ENVIRONMENT_VARIABLE}
    restart: unless-stopped
